package com.medsupply.platform.modules.warehouse.model;

/**
 * Enterprise status enum for warehouse stock transfers.
 */
public enum WarehouseTransferStatus {
    PENDING,
    APPROVED,
    REJECTED,
    CANCELLED
}
